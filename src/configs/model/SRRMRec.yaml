embedding_size: 64
feat_embed_dim: 64
weight_size: [64, 64]
learning_rate_scheduler: [0.96, 50]
lambda_coeff: 0.9
learning_rate: 0.001
n_mm_layers: 1
n_ui_layers: 2
ssl_temp: 0.2
reg_weight: 0.001
knn_k: 10
mm_image_weight: 0.1

ssl_weight: 0.05
beta: 1.5
reliability_metric: 'AA'

hyper_parameters: ["ssl_weight","beta","reliability_metric"]
